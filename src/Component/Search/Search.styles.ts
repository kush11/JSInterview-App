import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  input: {
    height: 40,
    margin: 6,
    borderWidth: 1,
    padding: 10,
    borderRadius: 8,
    borderColor: '#DAE2E6',
    color: '#000000',
    marginTop: 16,
  },
});
